'use strict';

describe('quizApp.results-page module', function() {

  beforeEach(module('quizApp.results-page'));

  describe('results-page controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var resultsPageCtrl = $controller('ResultsPageCtrl');
      expect(resultsPageCtrl).toBeDefined();
    }));

  });
});