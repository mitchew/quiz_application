'use strict';

describe('quizApp.main-page module', function() {

  beforeEach(module('quizApp.main-page'));

  describe('main-page controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var mainPageCtrl = $controller('MainPageCtrl');
      expect(mainPageCtrl).toBeDefined();
    }));

  });
});